<nav class="navbar navbar-default navbar-fixed-top">
	<div class="container-fluid">
		<div class="navbar-header">

			<!-- This button shows only on small screens -->
			<button type="button"
			class="navbar-toggle collapsed"
			data-toggle="collapse"
			data-target="#bs-example-navbar-collapse-1"
			aria-expanded="false">
				<span class="sr-only"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>

			<%= link_to 'KMForum', root_path, class: 'pull-left logo' %>

		</div>

		<!-- navbar actual body -->
		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
			<ul class="nav navbar-nav navbar-right">

				<% if logged_in? %>
					<li class="<%= 'active' if params[:action] == 'index' and params[:controller] == 'users' %>">
						<a href="<%= users_path %>">
							Users<span class="sr-only">Users</span>
						</a>
					</li>
				<% end %>

				<li class="<%= 'active' if params[:action] == 'contact' %>">
					<a href="<%= contact_path %>">
						Contact<span class="sr-only">Contact</span>
					</a>
				</li>

				<li class="<%= 'active' if params[:action] == 'about' %>">
					<a href="<%= about_path %>">
						About<span class="sr-only">About</span>
					</a>
				</li>

				<% if logged_in? %>
					<li class="dropdown">
						<a href="#"
						class="dropdown-toggle"
						data-toggle="dropdown"
						role="button"
						aria-haspopup="true"
						aria-expanded="false">
							Account<span class="caret"></span>
						</a>
						<ul class="dropdown-menu">
							<li>
								<%= link_to current_user do %>
									<span class="glyphicon glyphicon-user" aria-hidden="true"></span> Show profile
								<% end %>
							</li>
							<li>
								<%= link_to edit_user_path(current_user) do %>
								<span class="glyphicon glyphicon-cog" aria-hidden="true"></span> Account settings
							<% end %>
							</li>
							<li>
								<%= link_to logout_path, method: :delete do %>
									<span class="glyphicon glyphicon-log-out" aria-hidden="true"></span> Log out
								<% end %>
							</li>
						</ul>
					</li>
				<% else %>
					<li>
						<%= link_to login_path do %>
							<span class="glyphicon glyphicon-log-in" aria-hidden="true"></span> Log in
						<% end %>
					</li>
				<% end %>
			</ul>
		</div>
	</div>
</nav>
