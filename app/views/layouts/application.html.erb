<!DOCTYPE html>
<html>
  <%= render 'layouts/head' %>
  <body>
  	<div class="container">
  		<%= render 'layouts/navbar' %>
  		<div class="km-body-container">

        <%= render 'shared/notices' %>

    		<%= yield %>
    	</div>

      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
