<!DOCTYPE html>
<html>
  <%= render 'layouts/head' %>
  <body>
  	<div class="container">
  		<%= render 'layouts/navbar' %>
  		<div class="km-body-container">
    		<%= yield %>
    	</div>
    </div>
  </body>
</html>
