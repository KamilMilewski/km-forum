<% provide :title, @topic.title %>

<hr>

<%= render 'new_post_button' %>

<hr>
  <%= will_paginate @posts, renderer: BootstrapPagination::Rails %>
<hr>

<%= if params[:page] == nil || params[:page] == 1 then render 'topic' end %>

<%= render 'posts' %>

<hr>
  <%= will_paginate @posts, renderer: BootstrapPagination::Rails %>
<hr>

<%= render 'new_post_button' %>
